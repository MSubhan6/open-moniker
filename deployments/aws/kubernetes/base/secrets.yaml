apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
  namespace: moniker
type: Opaque
stringData:
  # These are placeholders - must be overridden in overlays with actual values
  # In production, use external-secrets or sealed-secrets
  telemetry_password: PLACEHOLDER_PASSWORD
