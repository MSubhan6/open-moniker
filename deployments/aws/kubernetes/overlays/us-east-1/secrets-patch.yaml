apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
  namespace: moniker
type: Opaque
stringData:
  # In production, retrieve from AWS Secrets Manager or use external-secrets operator
  # This is populated by the deployment script from Terraform outputs
  telemetry_password: ${AURORA_PASSWORD}
