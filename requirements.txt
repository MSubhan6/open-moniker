# Core dependencies for Render deployment
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
pyyaml>=6.0.0
# Optional: redis>=5.0.0 (for query caching)
# Optional: snowflake-connector-python (for Snowflake adapter)
# Optional: oracledb (for Oracle adapter)
# Optional: pyodbc (for MSSQL adapter)
